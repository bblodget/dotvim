Installation:

	git clone https://github.com/bblodget/dotvim.git ~/.vim

Create symlinks:

	ln -s ~/.vim/exrc ~/.exrc

	ln -s ~/.vim/vimrc.unix ~/.vimrc	(for unix eol)
	ln -s ~/.vim/vimrc.win ~/.vimrc		(for win eol)

